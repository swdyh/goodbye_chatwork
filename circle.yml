deployment:
  rubygems:
    branch: master
    commands:
      - echo $RUGYGEMS_ORG_CREDENTIALS > ~/.gem/credentials; chmod 0600 ~/.gem/credentials
      - git config user.name $RUBYGEMS_USERNAME
      - git config user.email $RUBYGEMS_EMAIL
      - bundle exec rake build
      - bundle exec rake release
